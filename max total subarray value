class Solution {
    public long maxTotalValue(int[] nums, int k) {
        Arrays.sort(nums);
        int n=nums.length;
        int dif=nums[n-1]-nums[0];
        long maxx=(long)dif*k;
        return maxx;
        
    }
}
